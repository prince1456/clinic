<%= simple_form_for [@post, @comment], remote: true,role: 'form' do |f| %>
  <div class="form-group">
    <%= f.input_field :content, class:'form-control', label: false%>
  </div>
  <%= f.button :submit, class:'ntm btn-default' %>
<% end %>
