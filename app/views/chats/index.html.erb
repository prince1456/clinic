<div class="container">
    <div class="col-md-2 col-sd-2 col-xs-10">
      <ul>
       <% if @existing_chats_users %>
         <% @existing_chats_users.each do |other_user| %>
          <% if other_user != current_user %>
            <li>
              <%= link_to(user_chats_path(current_user, :other_user => other_user.id), method: :post) do %>
              <%= image_tag other_user.profile.image %>
              <% end %>
            </li>
           <% end %>
         <% end %>
        <% end %>
      </ul>
      All Users:
      <ul>
        <% User.all.each do |other_user| %>
         <% if other_user != current_user %>
           <li>
             <%= link_to(user_chats_path(current_user, :other_user => other_user.id), method: :post) do %>
             <%= image_tag other_user.profile.image %>
             <% end %>
           </li>
          <% end %>
        <% end %>
      </ul>

    </div>
    <div class="col-md-10 col-sd-10 col-xs-10"></div>
</div>
